services:
  app:
    build: .
    ports:
      - "3080:3000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
    mem_limit: 256m
    mem_reservation: 128m
